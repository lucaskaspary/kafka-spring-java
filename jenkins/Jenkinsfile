#!/usr/bin/env groovy
pipeline {
    agent any
    stages{
        stage('Build') {
            agent {
                docker {
                    image 'gradle'
                }
            }
            steps {
                sh "gradle build"
            }
        }
    }
}